{
  "name": "a-frame-target-practice",
  "version": "0.1.0",
  "description": "A WebVR FPS Game",
  "scripts": {
    "build": "cross-env NODE_ENV=production webpack",
    "start": "webpack-dev-server --host 0.0.0.0 --port=5000 --progress --colors --hot -d --inline",
    "lint": "semistandard -v | snazzy"
  },
  "license": "MIT",
  "semistandard": {
    "ignore": [
      "**/vendor/**"
    ]
  },
  "devDependencies": {
    "cross-env": "^3.0.0",
    "semistandard": "^9.0.0",
    "snazzy": "^4.0.1",
    "webpack": "^1.13.2",
    "webpack-dev-server": "^1.16.1"
  }
}
